# 実務補習第8回考査問題データの格納ガイド

## 📁 データ格納場所

### 推奨ディレクトリ構造

```
/data
  /jissen-hoshu
    /dai8kai
      /kansa (監査)
        /2025.json
        /2024.json
        /2023.json
        ...
```

## 📋 JSONファイルの格納方法

### 格納場所

**各年度のJSONファイルは以下の場所に格納してください：**

```
/data/jissen-hoshu/dai8kai/kansa/[年度].json
```

### 例

- 2025年度：`/data/jissen-hoshu/dai8kai/kansa/2025.json`
- 2024年度：`/data/jissen-hoshu/dai8kai/kansa/2024.json`
- 2023年度：`/data/jissen-hoshu/dai8kai/kansa/2023.json`

### JSONファイルの命名規則

- **形式**：`[年度].json`
- **年度**：4桁の西暦（例：2025, 2024, 2023）

## 🔧 新しい年度のデータを追加する方法

### 1. JSONファイルを作成

`/data/jissen-hoshu/dai8kai/kansa/` ディレクトリに新しい年度のJSONファイルを作成します。

例：2024年度を追加する場合
- ファイル名：`2024.json`
- パス：`/data/jissen-hoshu/dai8kai/kansa/2024.json`

### 2. JSONファイルの構造

既存の `2025.json` を参考に、以下の構造でデータを作成してください：

```json
{
  "year": 2024,
  "examInfo": {
    "title": "監査総合グループ第8回考査",
    "date": "2024年3月17日（日）実施",
    "examTime": "2時間",
    "type": "監査総合グループ"
  },
  "instructions": {
    "items": [...]
  },
  "questions": [...]
}
```

### 3. 一覧ページに年度を追加

`/app/jissen-hoshu/dai8kai/kansa/page.tsx` の `availableYears` 配列に新しい年度を追加：

```typescript
const availableYears = [
  { year: 2025, date: "2025年3月16日（日）実施" },
  { year: 2024, date: "2024年3月17日（日）実施" }, // 追加
  // ...
]
```

### 4. 年度別ページにデータを追加

`/app/jissen-hoshu/dai8kai/kansa/[year]/page.tsx` の `examDataMap` に新しい年度のデータを追加：

```typescript
import examData2024 from '../../../../../../data/jissen-hoshu/dai8kai/kansa/2024.json'

const examDataMap: Record<number, ExamData> = {
  2025: examData2025 as ExamData,
  2024: examData2024 as ExamData, // 追加
  // ...
}
```

## 📝 データの表示

### 一覧ページ

- **URL**: `/jissen-hoshu/dai8kai/kansa`
- **機能**: 利用可能な年度の一覧を表示

### 年度別詳細ページ

- **URL**: `/jissen-hoshu/dai8kai/kansa/[年度]`
- **例**: `/jissen-hoshu/dai8kai/kansa/2025`
- **機能**: 選択した年度の問題を表示

## 🎯 まとめ

1. **格納場所**: `/data/jissen-hoshu/dai8kai/kansa/[年度].json`
2. **命名規則**: `[年度].json` (4桁の西暦)
3. **新しい年度を追加する場合**:
   - JSONファイルを作成
   - 一覧ページの `availableYears` に追加
   - 年度別ページの `examDataMap` に追加

## 📌 注意事項

- JSONファイルは必ず `/data/jissen-hoshu/dai8kai/kansa/` ディレクトリに格納してください
- ファイル名は年度（4桁の西暦）を使用してください
- 年度を追加したら、必ず一覧ページと年度別ページの両方に追加してください








